<% data = [%w[名称 描述 金额 限制 价格 开始时间 结束时间 代金券使用数 代金券获得数 代金券总数]] + @events.map { |event| [event.name, event.description, event.amount, event.limitation, event.price, event.started_at && event.started_at.to_s(:db), event.ended_at && event.ended_at.to_s(:db), event.vouchers.active.used.count, event.vouchers.active.obtain.count, event.vouchers.active.count] } %><%= raw "\357\273\277#{data.map(&:to_csv).join}" %>
