<div class="modal-header">
  <button type="button" class="close" data-dismiss="rightPanel">×</button>
  <h4 class="modal-title">退款</h4>
</div>
<div class="modal-body">
  <%= form_for @unit, as: :unit, :url => transfer_admin_shop_unit_path(@unit), :html => {:method => :put} do |f| %>
      <%= f.error_messages %>
      <%= label_tag 'sms[success]', "短信通知" %>
      <%= check_box_tag 'sms[success]', '1', @unit.return_phone?, :disabled => !(@unit.return_phone?) %>
      <%= label_tag 'sms[success]', @unit.return_phone? ? "#{h @unit.return_phone}
              尊敬的用户您好：您的退款已汇出，到账时间根据银行系统接转周期约1-7个工作日，可致电银行查询到账时间。
              如遇疑问请您致电客服电话查询反馈。感谢您的支持，祝您生活愉快！[珀丽莱]" : "" %>
      <%= f.submit "提交", class: 'btn btn-xs btn-success' %>
  <% end %>
</div>
