<% data = [%w[ID 产品ID 产品名称 产品品牌  一级分类 二级分类 编号 尺寸 ERP尺寸 交易ID]] + @items.map { |item| [item.id, item.product_id, item.product && item.product.name, item.product && item.product.brand && item.product && item.product.brand.name, item.product && item.product.category1 && item.product.category1.name, item.product && item.product.category2 && item.product.category2.name, "\t#{item.identifer}", item.measure, item.original_measure, item.trade_id] } %><%= raw "\357\273\277#{data.map(&:to_csv).join}" %>
